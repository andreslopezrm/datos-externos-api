export interface AvanceNacional {
  capturadas: Capturadas;
  participacionCiudadana: ParticipacionCiudadana;
  contabilizadas: Contabilizadas;
  ultimoCorte: UltimoCorte;
  urlBaseDatos: string;
}

export interface Capturadas {
  total: string;
  porcentaje: string;
  esperadas: string;
}

export interface Contabilizadas {
  total: string;
  porcentaje: string;
}

export interface ParticipacionCiudadana {
  porcentaje: string;
}

export interface UltimoCorte {
  fecha: string;
  hora: string;
}
export interface AvanceNacional {
  capturadas: Capturadas;
  participacionCiudadana: ParticipacionCiudadana;
  contabilizadas: Contabilizadas;
  ultimoCorte: UltimoCorte;
  urlBaseDatos: string;
}

export interface Capturadas {
  total: string;
  porcentaje: string;
  esperadas: string;
}

export interface Contabilizadas {
  total: string;
  porcentaje: string;
}

export interface ParticipacionCiudadana {
  porcentaje: string;
}

export interface UltimoCorte {
  fecha: string;
  hora: string;
}
